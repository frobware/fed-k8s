#cloud-config

growpart:
  mode: auto
  devices: ['/']

chpasswd:
  list: |
    cloud-user:password
  expire: false

packages:
  - NetworkManager

ssh_pwauth: True
disable_root: false

package_reboot_if_required: false
package_upgrade: false

power_state:
  mode: reboot
  condition: True

#PATH=/data/src/github.com/openshift/origin/_output/local/bin/linux/amd64:$PATH
#export PATH
#type -p direnv &>/dev/null && eval "$(direnv hook bash)"
#type -p oc &>/dev/null && source <(oc completion bash)
#type -p kubectl &>/dev/null && source <(kubectl completion bash)
