#cloud-config

growpart:
  mode: auto
  devices: ['/']

chpasswd:
  list: |
    root:root
    ubuntu:password
    fedora:password
  expire: false

# packages:
#   - bind-utils
packages:
  - openssh-server
  - NetworkManager

ssh_pwauth: True
disable_root: false

package_reboot_if_required: false
package_upgrade: false

# dnf copr enable -y ariscop/eatmydata
# https://copr.fedorainfracloud.org/coprs/ariscop/eatmydata/

power_state:
  mode: reboot
  condition: True
